# Away Together

Preserve the pixel-art personas and existing travel interface. The editable production authority is the parent project; synchronize runtime changes rather than diverging silently.

The app runs frozen DeBERTa V2 under app-quality gates. V2 scored 95.28% versus Jev 98.61% on the same synthetic travel test. It beat V1, not Jev. Never edit frozen inference files, weights, hashes or historical results to support a claim.

apps/public-demo uses recorded model observations and browser rules. Never present it as live model inference. The original build used Codex; Claude Opus helped with later upgrades.

Original code is MIT. Preserve upstream licenses. Never commit credentials, environments, caches or large weights. Use companion-v2 for the current local runtime assets.
