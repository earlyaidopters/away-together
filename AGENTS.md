# Away Together

Keep the pixel-art traveller personas, captions, rounded cards and sage/terracotta palette. apps/agency is the working demo; apps/explainer is the separate filming walkthrough. Update both only when requested. Canonical source at export lives in the YouTube build-your-own-jev project; avoid divergent edits without synchronizing.

Do not modify frozen checkpoint hashes, frozen inference source or historical results to support a story. The latest V2 challenger lost to Jev and was not promoted; V1 stays active. The vision branch integrates pretrained OpenJev/DiffusionGemma. Credit original contributions and preserve all upstream licenses.

Never commit credentials, environments, caches, large weights or ZIPs. Use private releases for companion artifacts. Restore the companion before running agency integration tests; see docs/SETUP.md. Do not change private repository visibility without an explicit user request.
