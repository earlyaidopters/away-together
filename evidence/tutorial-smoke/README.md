# Tutorial execution receipts

Real pinned ModernBERT baseline, one training epoch, paired test and new-input prediction on Apple silicon. Only eight decisions per split were used. These receipts prove that the stages execute; they do not establish model quality or reproduce the historical training scores. Both models scored 37.5% on this tiny test. Checkpoint weights are deliberately excluded. Run the documented commands in a new workspace to make your own outputs.
