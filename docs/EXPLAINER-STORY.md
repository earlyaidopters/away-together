# Visual follow-along tutorial



Authority: FILMING-GUIDE.md. 24 scenes, 1,807 spoken words, planned 15:00 including visual and demo holds. Viewers give Claude or Codex prompts and links.



1. See how a photo changes the answer (0:00–0:55): Scene `#demo` at localhost:8770. Start with the live persona demo already visible. Deliver the Jev/local/images claim over the app. Run Maya with the stairs photo by 0:10; remove it and run again. Point at review by 0:25. Briefly show the full agency grid. If either request stalls, preserve real elapsed time and cut the wait transparently.

2. Build it one step at a time (0:55–1:30): Scene `#why` at localhost:8770. Point to the four-stage visual path.

3. Start with a model that can classify text (1:30–2:15): Scene `#choose-model` at localhost:8770. Open the actual model card. Point to owner, task, weights and pinned revision; use the card as a navigation surface, not a fabricated screenshot.

4. Measure it before you train it (2:15–3:00): Scene `#baseline` at localhost:8770. Point at the before bar first, then after. Keep development and separate final-test caveat visible. Open the JSON receipt.

5. Ask AI to download the model (3:00–3:40): Scene `#download` at localhost:8770. Copy the prompt and model link. Paste them into Claude Code or Codex with local tool access. Show a real first example if executing; the animation only explains the download.

6. Tell it the job you want done (3:40–4:15): Scene `#build` at localhost:8770. Show the holiday photo, booking terms and Maya beside the copyable brief.

7. Show it what “a good match” means (4:15–5:00): Scene `#travel-brief` at localhost:8770. Underline the label contract and four definitions one by one while explaining. These are real travel requirements from the current model.

8. Give Codex the complete brief (5:00–5:55): Scene `#prompt` at localhost:8770. Click Job, Data, Training and Final test tabs. Open the complete prompt page and show that the download has all eight sections. Underlines identify editable task fields.

9. Give it three choices (5:55–6:25): Scene `#labels` at localhost:8770. Click Cash back, then Hotel credit. Point to the mapping between the teaching labels and actual output labels.

10. Pair each example with an answer (6:25–7:00): Scene `#training` at localhost:8770. Open actual train-00000 excerpt. Download the full record. Point at input, question, answer; show the separate held-out file only as data provenance.

11. Keep some examples for the final test (7:00–7:35): Scene `#data-split` at localhost:8770. Play the practice, settings and sealed-test folders appearing in order.

12. Train it using checked examples (7:35–8:10): Scene `#practice` at localhost:8770. Play the existing training animation. Copy the training prompt below it when moving to execution.

13. Test it on examples it hasn’t seen (8:10–8:45): Scene `#test` at localhost:8770. Play identical unseen examples flowing to both models. Copy the test prompt.

14. Look at an answer it got wrong (8:45–9:15): Scene `#failure` at localhost:8770. Show case …0036 arrival excerpt and both saved outputs. Open full JSON to show the input is longer than the excerpt.

15. Compare its answers with Jev's (9:15–9:45): Scene `#results` at localhost:8770. Show common-axis bars; point to text-only scope and distinguish demo V1 from later V2. Do not animate a V1→V2 improvement arrow.

16. Ask it to try a new policy (9:45–10:25): Scene `#predict` at localhost:8770. Show the cancellation letter and prompt. Paste into a coding assistant for actual saved-model inference; do not substitute the assistant's own answer.

17. Connect the models with code (10:25–10:55): Scene `#map` at localhost:8770. Point to text reader, pretrained photo reader and app rules in order; keep Maya visible when returning to the agency.

18. Show how a picture becomes evidence (10:55–11:35): Scene `#vision` at localhost:8770. Play photo, pixel grid, feature answers and Maya's requirement in order. The grid illustrates pixel input, not an exact architecture trace. Open photo lab for fresh inference.

19. Check what the photo can tell you (11:35–12:05): Scene `#boundary` at localhost:8770. Show pond, ask the three questions and reveal the conclusion. Refer back to the missing stairs photo.

20. Check each person's requirements (12:05–12:35): Scene `#rules` at localhost:8770. Play budget/terms/photo gates. In the actual agency open a traveller drawer and show the clause/photo that caused decline or review.

21. Ask AI to get the demo running (12:35–13:15): Scene `#first-run` at localhost:8770. Show the Kyoto demo illustration and copy repo plus setup prompt. Open the real agency after the assistant starts it.

22. See where the costs come from (13:15–13:50): Scene `#cost` at localhost:8770. Animate the build receipt, local computer and per-check model API fee. No fabricated total or savings percentage.

23. Change one example yourself (13:50–14:25): Scene `#repurpose` at localhost:8770. Edit support request, choose its human label, add it and download JSONL. Run prepare_task.py on the saved sample. Show held-out separation in the adaptation guide.

24. Point AI at the repo and start building (14:25–15:00): Scene `#take-it` at localhost:8770. Show the source kit and editable task prompt. Copy prompt plus the single repository URL.
