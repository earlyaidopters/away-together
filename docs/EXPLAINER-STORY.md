# Follow-along A–Z tutorial

Authority: FILMING-GUIDE.md. 29 scenes, 2,263 spoken words, planned 18:40 with command and demo holds. The latest request for a comprehensive walkthrough supersedes the earlier 5–10 minute summary.

1. See how a photo changes the answer (0:00–0:55): Scene `#demo` at localhost:8770. Start with the live persona demo already visible. Deliver the Jev/local/images claim over the app. Run Maya with the stairs photo by 0:10; remove it and run again. Point at review by 0:25. Briefly show the full agency grid. If either request stalls, preserve real elapsed time and cut the wait transparently.
2. Follow the build from start to finish (0:55–1:40): Scene `#why` at localhost:8770. Trace the six steps. Open the full guide briefly, then return to the first step.
3. Start with an existing classifier (1:40–2:25): Scene `#choose-model` at localhost:8770. Open the actual model card. Point to owner, task, weights and pinned revision; use the card as a navigation surface, not a fabricated screenshot.
4. Measure it before you train it (2:25–3:10): Scene `#baseline` at localhost:8770. Point at the before bar first, then after. Keep development and separate final-test caveat visible. Open the JSON receipt.
5. Check your computer first (3:10–3:40): Scene `#setup` at localhost:8770. Show tested M5 Max/128 GB host and separate text/image requirements. Open Start here prerequisites briefly.
6. Download the model to your computer (3:40–4:25): Scene `#download` at localhost:8770. Show the source repository in Codex, then run the download command in its terminal. Show actual printed revision and cache path. If cached, say so; do not stage a fresh download.
7. Tell Codex exactly what you want (4:25–5:05): Scene `#build` at localhost:8770. Point at each requirement before showing the full prompt. Keep Codex visibly open on the repository when filming.
8. Write the rules for your travel job (5:05–5:50): Scene `#travel-brief` at localhost:8770. Underline the label contract and four definitions one by one while explaining. These are real travel requirements from the current model.
9. Give Codex the complete brief (5:50–6:45): Scene `#prompt` at localhost:8770. Click Job, Data, Training and Final test tabs. Open the complete prompt page and show that the download has all eight sections. Underlines identify editable task fields.
10. Give it three choices (6:45–7:15): Scene `#labels` at localhost:8770. Click Cash back, then Hotel credit. Point to the mapping between the teaching labels and actual output labels.
11. Pair each example with an answer (7:15–7:50): Scene `#training` at localhost:8770. Open actual train-00000 excerpt. Download the full record. Point at input, question, answer; show the separate held-out file only as data provenance.
12. Keep three separate sets of examples (7:50–8:30): Scene `#data-split` at localhost:8770. Show three real data files. Point at scenario ID and template family in source records. Do not inspect final answer errors until the model is frozen.
13. Train in a separate working folder (8:30–9:15): Scene `#workspace` at localhost:8770. Run prepare in the repository root; show created data folder and manifest. Demonstrate that a second prepare refuses the existing folder.
14. Train it using checked examples (9:15–9:45): Scene `#practice` at localhost:8770. Play the two-lane HyperFrame. Hold at the wrong guess, checked label and saved-settings update. Point to unchanged settings in the normal-check lane.
15. Run the baseline, then start training (9:45–10:35): Scene `#train-run` at localhost:8770. Show real baseline receipt, actual epoch log and saved checkpoint files from the verified tutorial run. Clearly label eight-decision smoke check; do not present it as the historical full training run.
16. Test it on examples it hasn't seen (10:35–11:05): Scene `#test` at localhost:8770. Show practice and final-test objects. Open the held-out example and explain its split, then return to the diagram.
17. Compare both models on the same test (11:05–11:50): Scene `#test-run` at localhost:8770. Execute the small verification test if needed; open its saved report. Show rows for both models on the same IDs. Then use historical full-study failure in next scene.
18. Look at an answer it got wrong (11:50–12:20): Scene `#failure` at localhost:8770. Show case …0036 arrival excerpt and both saved outputs. Open full JSON to show the input is longer than the excerpt.
19. Compare its answers with Jev's (12:20–12:50): Scene `#results` at localhost:8770. Show common-axis bars; point to text-only scope and distinguish demo V1 from later V2. Do not animate a V1→V2 improvement arrow.
20. Try your saved model on new text (12:50–13:35): Scene `#predict` at localhost:8770. Download the actual input, run predict, read its returned sentence. Change cash refund to hotel credit and explain the expected change without guaranteeing it.
21. Connect the models with code (13:35–14:05): Scene `#map` at localhost:8770. Point to text reader, pretrained photo reader and app rules in order; keep Maya visible when returning to the agency.
22. Add a model that reads images (14:05–14:40): Scene `#vision` at localhost:8770. Play photo→observation→app. Open photo-lab.html, choose a local image, run it and inspect actual answers and image hash. Keep the service running before filming.
23. Check what the photo can tell you (14:40–15:10): Scene `#boundary` at localhost:8770. Show pond, ask the three questions and reveal the conclusion. Refer back to the missing stairs photo.
24. Check each person's requirements (15:10–15:40): Scene `#rules` at localhost:8770. Play budget/terms/photo gates. In the actual agency open a traveller drawer and show the clause/photo that caused decline or review.
25. Run it before changing it (15:40–16:20): Scene `#first-run` at localhost:8770. Open Start here. Show source and restored files, run first_run.py, start agency in a spare terminal if needed, inspect service status and run a fresh holiday. No fake installation animation.
26. Work out what it costs to run (16:20–16:50): Scene `#cost` at localhost:8770. Show build/setup and local-running cost columns, then separately labeled historical timings. Point to fresh demo time rather than implying benchmark-equivalent workloads.
27. Change one example yourself (16:50–17:25): Scene `#repurpose` at localhost:8770. Edit support request, choose its human label, add it and download JSONL. Run prepare_task.py on the saved sample. Show held-out separation in the adaptation guide.
28. Keep the files that let you run it again (17:25–18:05): Scene `#handover` at localhost:8770. Show the actual workshop file tree and the README/prompt folder. Explain which files a different domain changes.
29. Start with a working example (18:05–18:40): Scene `#take-it` at localhost:8770. Open Start here and the question index. Show current access/permission status accurately; use only the delivered link once verified. Keep original creator/model credits visible.
